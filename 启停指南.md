# WeLink 启停指南

## 1. 当前状态

WeLink 当前已经具备基础可用性，下面这些核心链路已在当前仓库和环境中验证通过：

- 单聊收发、历史消息、离线消息、会话已读
- 群聊收发、小中群写扩散、大群读扩散
- `message_outbox` + `outbox_pending` 的跨实例投递
- 8 个分片 MySQL + 1 个主 MySQL 的干净重建
- 双实例 IM 进程并行运行与真实跨实例群聊压测

当前结论适合用作开发、联调、功能回归和容量收敛，不代表已经达到生产 SLA。

## 2. 运行模式

项目保留两种启动模式：

- `sharding`：默认模式，使用 8 个分片 MySQL + 1 个主 MySQL
- `singledb`：本地调试/回退模式，使用单 MySQL `welink` 库

当前推荐使用 `sharding`，因为仓库中的分库分表、群聊扩散、outbox 路径都以它为主线验证。

## 3. 依赖环境

需要的软件与默认端口：

- JDK 21
- Maven 3.9+
- Node.js 20+
- Docker Desktop
- MySQL: `3307-3315` 或单机 `3307`
- Redis: `6379`
- Kafka: `9092`
- MinIO: `9000/9001`
- HTTP API: `8080`
- WebSocket: `8081`
- Frontend Dev Server: `3000`

## 4. 启动中间件

### 4.1 默认分片模式

```bash
cd g:/WeLink
docker compose -f docker-compose.multi-mysql.yml up -d
```

该编排会启动：

- `mysql-shard-0` 到 `mysql-shard-7`
- `mysql-main`
- `redis`
- `kafka`
- `minio`

### 4.2 单库调试模式

```bash
cd g:/WeLink
docker compose -f docker-compose.yml up -d
```

说明：

- 该文件仅用于 `--spring.profiles.active=singledb`
- 如果直接使用默认 `sharding` profile，请不要配合这个 compose

## 5. 启动后端

### 5.1 默认分片模式

```bash
cd g:/WeLink
mvn -DskipTests package
java -jar target/WeLink-0.0.1-SNAPSHOT.jar
```

默认会读取：

- `application.properties`
- `application-sharding.properties`
- `sharding-config.yaml`

### 5.2 单库模式

```bash
cd g:/WeLink
java -jar target/WeLink-0.0.1-SNAPSHOT.jar --spring.profiles.active=singledb
```

## 6. 启动前端

```bash
cd g:/WeLink/frontend
npm install
npm run dev
```

开发期默认通过 Vite 反代：

- `/api/v1/*` -> `http://localhost:8080`
- WebSocket -> `ws://localhost:8081/ws`

## 7. 停止服务

停止 compose：

```bash
docker compose -f docker-compose.multi-mysql.yml down
```

清空卷并彻底重建：

```bash
docker compose -f docker-compose.multi-mysql.yml down -v
docker compose -f docker-compose.multi-mysql.yml up -d
```

停止单库 compose：

```bash
docker compose -f docker-compose.yml down
```

## 8. 快速自检

后端健康检查：

```bash
curl http://localhost:8080/actuator/health
```

Prometheus 指标：

```bash
curl http://localhost:8080/actuator/prometheus
```

前端入口：

- `http://localhost:3000`

MinIO 控制台：

- `http://localhost:9001`

## 9. 常见问题

### 9.1 启动后提示找不到分片表

通常是用了默认 `sharding` profile，但只启动了 `docker-compose.yml`。

解决方式：

- 使用 `docker-compose.multi-mysql.yml`
- 或切到 `--spring.profiles.active=singledb`

### 9.2 主库里出现了分片消息表

说明初始化脚本或环境被错误复用了。先执行：

```bash
docker compose -f docker-compose.multi-mysql.yml down -v
docker compose -f docker-compose.multi-mysql.yml up -d
```

然后核验 `3315/welink` 只保留非分片表。

### 9.3 第二个 IM 实例如何启动

见 `document/WeLink多实例部署说明.md`。

### 9.4 压测入口在哪

见 `jmeter/README.md`、`document/并发性能测试设计文档.md`、`document/分布式压测部署指南.md`。
